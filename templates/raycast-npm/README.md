# Raycast npm script template

This template keeps canonical script names while allowing compatibility aliases (`type-check`) during migration.

Keep `build` and `lint` aligned with Raycast Store requirements.
