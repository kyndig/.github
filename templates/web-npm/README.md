# Web npm script template

For SvelteKit repos (for example TF), keep `svelte:check` namespaced and use `check` as the aggregate quality entrypoint.

Replace command bodies to match repository tooling.
